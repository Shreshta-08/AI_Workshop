# AI_Workshop
findfirst.js

# Project Description
It searches through a text file line by line, looking for lines that contain a given pattern (like grep), and stops once it's found the first N matching lines (like head limiting output) — printing only those matches to the console.


# AI Reflection
Using AI today helped me debug a lot of issues in my code and also gave me a great idea of what commands such as grep and head. 
